name: First Workflow
on: workflow_dispatch
jobs: 
  first-job: 
    runs-on: ubuntu-latest
    steps: 
      - name: Print greeting
        run: |
          echo "Hello World!"
          echo "Does this work"
          uname -r
      - name: Print Goodbye
        run: echo "Done"
